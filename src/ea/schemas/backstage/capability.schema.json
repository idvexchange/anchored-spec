{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://anchored-spec.dev/schemas/backstage/capability.schema.json",
  "title": "Anchored Spec — Capability Entity",
  "description": "A business capability.",
  "allOf": [{ "$ref": "https://anchored-spec.dev/schemas/backstage/entity-envelope.schema.json" }],
  "properties": {
    "apiVersion": { "const": "anchored-spec.dev/v1alpha1" },
    "kind": { "const": "Capability" },
    "spec": {
      "type": "object",
      "required": ["owner"],
      "properties": {
        "status": { "type": "string" },
        "owner": { "type": "string" },
        "supportedBy": { "type": "array", "items": { "type": "string" } },
        "dependsOn": { "type": "array", "items": { "type": "string" } }
      },
      "additionalProperties": true
    }
  }
}
