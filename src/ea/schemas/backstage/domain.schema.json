{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://anchored-spec.dev/schemas/backstage/domain.schema.json",
  "title": "Backstage Domain Entity",
  "description": "A business domain grouping related systems.",
  "allOf": [{ "$ref": "https://anchored-spec.dev/schemas/backstage/entity-envelope.schema.json" }],
  "properties": {
    "apiVersion": { "const": "backstage.io/v1alpha1" },
    "kind": { "const": "Domain" },
    "spec": {
      "type": "object",
      "required": ["owner"],
      "properties": {
        "owner": { "type": "string", "description": "Owner entity reference." }
      },
      "additionalProperties": true
    }
  }
}
